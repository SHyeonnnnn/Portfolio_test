import React from 'react';
import Header from '../components/Header';

const netflix = () => {
    return (
        <div>
            <Header></Header>
            netflix
        </div>
    );
};

export default netflix;